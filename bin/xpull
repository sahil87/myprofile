#!/bin/bash
# Pull files helper

# Add directories here:
DIRS=(
  $MYPROFILEDIR
  $LIFETRACKERDIR
  $BLOGDIR
  $DEVSHELLDIR
  $PROMPTPANTRYDIR
)

pull_dir() {
  echo "Pulling $1"
  cd "$1" && git fetch origin && git merge origin/master
}

for dir in "${DIRS[@]}"; do
  pull_dir "$dir"
done
