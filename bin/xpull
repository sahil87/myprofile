#!/bin/bash
# Pull files helper

# Add directories here:
DIRS=(
  $MYPROFILE_DIR
  $LIFETRACKER_DIR
  $BLOG_DIR
  $DEVSHELL_DIR
  $PP_DIR
)

pull_dir() {
  echo "Pulling $1"
  cd "$1" && git pull
}

for dir in "${DIRS[@]}"; do
  pull_dir "$dir"
done
