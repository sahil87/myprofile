#!/bin/bash
# Pull files helper

# Add directories here:
DIRS=(
  $MYPROFILEDIR
  $LIFETRACKERDIR
  $BLOGDIR
  $DEVSHELL
  $PROMPTPANTRYDIR
)

pull_dir() {
  echo "Pulling $1"
  cd "$1" && git pull
}

for dir in "${DIRS[@]}"; do
  pull_dir "$dir"
done
